@model projectsd.Models.User

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>User</h4>
        <hr />
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.id)

        <div class="form-group">
            @Html.LabelFor(model => model.PropertiesId, "PropertiesId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("PropertiesId", String.Empty)
                @Html.ValidationMessageFor(model => model.PropertiesId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.AuthId, "AuthId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("AuthId", String.Empty)
                @Html.ValidationMessageFor(model => model.AuthId)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ContactId, "ContactId", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("ContactId", String.Empty)
                @Html.ValidationMessageFor(model => model.ContactId)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
